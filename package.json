{
  "name": "zero-onboarding",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun --filter web dev",
    "build": "bun --filter web build"
  },
  "devDependencies": {
    "typescript": "~5.9.3"
  }
}
